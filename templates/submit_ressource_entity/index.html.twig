{% extends 'base.html.twig' %}

{% block title %}Hello SumbitRessourceEntityController!{% endblock %}

{% block body %}
    <div class="w-full h-full flex flex-col items-center space-y-10">
        <div id="app" class="py-4">
            <div class="text-4xl font-medium">{{ ressource_name }}</div>
        </div>

        <div class="w-1/4 w-full px-20 rounded-xl h-96 flex flex-col items-center justify-center bg-zinc-800 space-y-4">

            {{ form_start(
                form, {
                    'attr': {
                        'label': 'Prix moyen',
                        'class': 'h-full w-full flex flex-col items-center justify-center space-y-6'
                    },
                    'label_attr':{
                        'class': ''
                    }
                }
            ) }}
            {{ form_row(
                form.price, {
                    'attr': {
                        'label': 'Prix moyen',
                        'class': 'flex flex-col items-center w-full rounded-lg text-zinc-900 px-2 py-1 text-center text-lg'
                    },
                    'label_attr':{
                        'class': 'w-full flex justify-center mb-2 text-xl font-medium'
                    }
                }
            ) }}
            {{ form_row(
                form.coeff_pepite,{
                    'attr': {
                        'label': 'Prix moyen',
                        'class': 'flex flex-col items-center w-full rounded-lg text-zinc-900 px-2 py-1 text-center text-lg'
                    },
                    'label_attr':{
                        'class': 'w-full flex justify-center mb-2 text-xl font-medium'
                    }
                }
            ) }}

            {{ form_row(
                form.bonus, {
                    'attr': {
                        'label': 'Bonus',
                        'class': 'flex flex-col items-center w-full rounded-lg text-zinc-900 px-2 py-1 text-center text-lg'
                    },
                    'label_attr':{
                        'class': 'w-full flex justify-center mb-2 text-xl font-medium'
                    }
                }
            ) }}

            {{ form_row(
                form.valider, {
                    'attr': {
                        'class':'rounded-lg text-white bg-purple-800 px-4 py-2 text-lg hover:bg-purple-900'
                    }
                }
            ) }}

            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
